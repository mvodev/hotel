mixin switch( { isPositive=false, name="", }={} )
  .switch
    .switch__container
      label(class='switch__label')=name
        input(type='checkbox' class="switch__input" checked=isPositive)
        span(class='switch__slider-round') 